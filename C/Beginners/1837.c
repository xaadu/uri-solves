//1837

#include <stdio.h>
int main()
{
    int a,b,q,r,x,y;
    scanf("%d %d",&a,&b);
    if(b!=0)
    {
        x=a;
        y=b;
        if(x>0)
            printf("%d",)
        while(r!=0)
        {
            q=a/b;
            r=a%b;
            a=b;
            b=r;
        }
        printf("%d %d",q,x-(y*q));
    }
    return 0;
}

